import {useNavigate} from "react-router-dom";
import {Center, Loader} from "@mantine/core";

interface Prop {
	error: boolean,
}

export default function TwitchLoaded({error}: Prop) {
	let log;

	return (
		<>

		</>
	);
}